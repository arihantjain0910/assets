<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Requisitions</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        padding: 10px;
        background-color: #f8f9fa;
      }
      .container {
        max-width: 1600px;
        margin: auto;
      }
      h2 {
        margin-bottom: 20px;
        text-align: center;
      }
      .table-container {
        margin-top: 20px;
      }
      table {
        width: 100%;
        margin-bottom: 20px;
        border-collapse: collapse;
      }
      th,
      td {
        text-align: center;
        vertical-align: middle;
        padding: 12px;
        border: 1px solid #dee2e6;
      }
      th {
        background-color: #007bff;
        color: white;
      }
      tr:nth-child(even) {
        background-color: #f2f2f2;
      }
      tr:hover {
        background-color: #e9ecef;
      }
      .no-data {
        text-align: center;
        color: #6c757d;
        padding: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Your Requisitions</h2>
      <div class="table-container">
        <table class="table table-striped table-bordered">
          <thead>
            <tr>
                <th>Plant</th>
                <th>Profit Center</th>
                <th>Date Of Requisition</th>
                <th>Type</th>
                <th>Material Code</th>
                <th>Description</th>
                <th>Qty</th>
                <th>Purchase Type</th>
                <th>Total Cost</th>
                <th>Payback Period</th>>
              <th>Status 1</th>
              <th>Comments 1</th>
              <th>Status 2</th>
              <th>Comments 2</th>
              <th>Status 3</th>
              <th>Comments 3</th>
              <th>Status 4</th>
              <th>Comments 4</th>
            </tr>
          </thead>
          <tbody>
            <% if (requisitions.length === 0) { %>
            <tr>
              <td colspan="4" class="no-data">No requisitions found.</td>
            </tr>
            <% } else { %> <% requisitions.forEach(req => { %>
            <tr>
                <td><%= req.plant %></td>
                <td><%= req.profitCenter %></td>
                <td><%= new Date(req.dateOfRequisition).toLocaleDateString('en-GB') %></td>
                <td><%= req.type %></td>
                <td><%= req.materialCode %></td>
                <td><%= req.description %></td>
                <td><%= req.qty %></td>
                <td><%= req.purchaseType %></td>
                <td><%= req.totalCost %></td>
                <td><%= req.paybackPeriod %></td>
              <td><%= req.releaser1_approval_status %></td>
              <td><%= req.releaser1_comments %></td>
              <td><%= req.releaser2_approval_status %></td>
              <td><%= req.releaser2_comments %></td>
              <td><%= req.releaser3_approval_status %></td>
              <td><%= req.releaser3_comments %></td>
              <td><%= req.releaser4_approval_status %></td>
              <td><%= req.releaser4_comments %></td>
            </tr>
            <% }) %> <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
 

 